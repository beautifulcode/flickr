App.Views.UserView = Backbone.View.extend({

});

App.Views.PhotoView = Backbone.View.extend({

});

App.Views.PhotoSetView = Backbone.View.extend({

});

App.Views.ApplicationView = Backbone.View.extend({

  initialize: function(){
    console.log(this)
  }

});
